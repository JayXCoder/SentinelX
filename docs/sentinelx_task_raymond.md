# SentinelX — task.md for Raymond

## Owner

**Raymond**

## Assigned Scope

Raymond is responsible for frontend development together with Geng Xin.

Raymond focuses mainly on:

- frontend architecture
- dashboard implementation
- backend API integration
- real-time frontend data flow
- frontend state management
- dashboard functionality

Raymond owns the frontend application side that allows users to view, explore, filter and understand the intelligence generated by SentinelX.

---

# 1. Main Objective

Build the main SentinelX dashboard frontend using Next.js, TypeScript and Tailwind CSS v4.

The frontend must make the intelligence pipeline usable for real users by presenting:

- executive summaries
- live threat feeds
- competitor intelligence
- vendor risk scores
- GTM opportunities
- financial signals
- correlated events
- RAG answers
- alerts and notifications

The goal is to create a clean, fast and professional dashboard that connects directly with the FastAPI backend services built by Jay and Kai Zhe.

---

# 2. Technology Stack

Raymond’s assigned frontend stack:

- Next.js
- TypeScript
- Tailwind CSS v4
- React
- REST API integration
- WebSocket or Server-Sent Events
- Docker

Optional libraries:

- Axios or Fetch API wrapper
- Zustand or React Context
- Recharts
- TanStack Table
- React Hook Form
- Zod

---

# 3. Frontend Responsibilities

Raymond needs to build these frontend areas:

## 3.1 Frontend Project Structure

Responsible for:

- creating the Next.js app
- setting up TypeScript
- configuring Tailwind CSS v4
- creating clean folder structure
- preparing reusable layout structure

## 3.2 Dashboard Pages

Responsible for:

- Executive Overview page
- Threat Feed page
- Competitor Intelligence page
- Vendor Monitoring page
- Alerts page
- Intelligence Explorer page

## 3.3 Backend API Integration

Responsible for:

- connecting frontend to FastAPI endpoints
- building typed API services
- handling loading states
- handling error states
- displaying backend data cleanly

## 3.4 Real-Time Data Integration

Responsible for:

- receiving live updates from backend
- updating dashboard cards automatically
- showing incoming alerts
- handling WebSocket or SSE connection status

## 3.5 Frontend State Management

Responsible for:

- dashboard filters
- selected entity state
- selected time range
- user preferences
- live notification state

---

# 4. Suggested Folder Structure

```text
sentinelx-frontend/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── dashboard/
│   │   ├── page.tsx
│   │   ├── executive-overview/
│   │   │   └── page.tsx
│   │   ├── threat-feed/
│   │   │   └── page.tsx
│   │   ├── competitors/
│   │   │   └── page.tsx
│   │   ├── vendors/
│   │   │   └── page.tsx
│   │   ├── alerts/
│   │   │   └── page.tsx
│   │   └── intelligence-explorer/
│   │       └── page.tsx
├── components/
│   ├── layout/
│   │   ├── sidebar.tsx
│   │   ├── topbar.tsx
│   │   └── dashboard-layout.tsx
│   ├── dashboard/
│   │   ├── metric-card.tsx
│   │   ├── risk-card.tsx
│   │   ├── signal-feed.tsx
│   │   ├── score-chart.tsx
│   │   └── alert-list.tsx
│   ├── tables/
│   │   ├── data-table.tsx
│   │   ├── vendor-table.tsx
│   │   └── threat-table.tsx
│   └── ui/
│       ├── button.tsx
│       ├── input.tsx
│       ├── badge.tsx
│       ├── modal.tsx
│       └── loading.tsx
├── lib/
│   ├── api-client.ts
│   ├── websocket-client.ts
│   ├── utils.ts
│   └── constants.ts
├── hooks/
│   ├── use-dashboard-summary.ts
│   ├── use-threat-feed.ts
│   ├── use-vendor-risk.ts
│   ├── use-realtime-events.ts
│   └── use-filters.ts
├── stores/
│   ├── dashboard-store.ts
│   ├── alert-store.ts
│   └── filter-store.ts
├── types/
│   ├── api.ts
│   ├── signal.ts
│   ├── risk-score.ts
│   ├── vendor.ts
│   └── alert.ts
├── public/
├── Dockerfile
├── package.json
└── README.md
```

---

# 5. Main Pages To Build

## 5.1 Executive Overview Page

### Purpose

This page gives users a high-level summary of SentinelX intelligence.

### Must Display

- total intelligence signals
- active cyber threats
- top vendor risks
- top GTM opportunities
- critical alerts
- recent correlated events
- overall enterprise intelligence posture

### Components Needed

- summary metric cards
- risk overview chart
- recent alert feed
- top entities table
- intelligence summary panel

### API Dependencies

```text
GET /analytics/overview
GET /analytics/top-risks
GET /analytics/top-opportunities
GET /correlation/events
GET /risk-scores
```

---

## 5.2 Threat Feed Page

### Purpose

This page shows cybersecurity-related intelligence signals.

### Must Display

- cyber signals
- CVE alerts
- breach mentions
- exposed credential warnings
- phishing signals
- malware/ransomware signals

### Features

- filter by severity
- filter by date
- filter by affected entity
- search by keyword
- open signal details

### API Dependencies

```text
GET /agents/signals?type=cyber
GET /analytics/cyber-risk-summary
GET /risk-scores/type/cyber_exposure
```

---

## 5.3 Competitor Intelligence Page

### Purpose

This page shows market and competitor movement signals.

### Must Display

- competitor updates
- pricing changes
- product launches
- hiring signals
- customer sentiment
- GTM opportunities

### Features

- competitor filter
- signal type filter
- trend visualization
- opportunity score display

### API Dependencies

```text
GET /agents/signals?type=gtm
GET /analytics/market-movement-summary
GET /risk-scores/type/gtm_opportunity
```

---

## 5.4 Vendor Monitoring Page

### Purpose

This page shows vendor health and risk intelligence.

### Must Display

- vendor list
- vendor risk score
- vendor incident history
- vendor breach signals
- vendor downtime signals
- vendor relationship information

### Features

- vendor search
- risk level filter
- vendor detail modal
- score trend chart

### API Dependencies

```text
GET /agents/signals?type=vendor_risk
GET /analytics/vendor-risk-summary
GET /risk-scores/type/vendor_risk
GET /graph/entities?type=vendor
```

---

## 5.5 Alerts Page

### Purpose

This page manages alert visibility and notification settings.

### Must Display

- active alerts
- resolved alerts
- alert severity
- alert source
- alert timestamp

### Features

- mark alert as viewed
- filter alerts
- configure alert thresholds
- show notification channel settings

### API Dependencies

```text
GET /alerts
PATCH /alerts/{alert_id}
GET /analytics/top-risks
```

---

## 5.6 Intelligence Explorer Page

### Purpose

This page allows users to query and explore stored intelligence.

### Must Display

- RAG query input
- AI-generated answer
- supporting evidence
- related entities
- related events
- confidence score

### API Dependencies

```text
POST /rag/ask
POST /rag/query
GET /graph/entities
GET /graph/timeline/{entity_id}
```

---

# 6. TypeScript Types

Raymond should create frontend types that match backend responses.

## Intelligence Signal Type

```ts
export type IntelligenceSignal = {
  id: string;
  signal_type: 'cyber' | 'gtm' | 'financial' | 'vendor_risk' | 'osint' | 'executive_summary';
  category: string;
  title: string;
  summary: string;
  entities: string[];
  severity: number;
  confidence: number;
  source_reliability: number;
  evidence: string[];
  recommended_action?: string;
  created_at: string;
};
```

## Risk Score Type

```ts
export type RiskScore = {
  id: string;
  entity_id: string;
  entity_name: string;
  score_type: string;
  score_value: number;
  risk_level: 'low' | 'medium' | 'high' | 'critical';
  explanation: string;
  calculated_at: string;
};
```

## Correlated Event Type

```ts
export type CorrelatedEvent = {
  id: string;
  event_type: string;
  title: string;
  summary: string;
  involved_entities: string[];
  signal_ids: string[];
  correlation_reason: string;
  confidence: number;
  severity: number;
  first_seen: string;
  last_seen: string;
};
```

## RAG Answer Type

```ts
export type RagAnswer = {
  answer: string;
  confidence: number;
  supporting_evidence: string[];
  related_entities: string[];
  related_events: string[];
  recommended_action?: string;
};
```

---

# 7. API Client Requirements

Raymond should create a central API client.

## Required File

```text
lib/api-client.ts
```

## Responsibilities

- store base API URL
- handle request headers
- handle errors
- return typed responses
- support GET, POST, PATCH, DELETE

## Required API Functions

```text
getDashboardOverview()
getTopRisks()
getTopOpportunities()
getCyberSignals()
getGtmSignals()
getVendorSignals()
getRiskScores()
getCorrelatedEvents()
askRagQuestion()
getEntityTimeline()
```

---

# 8. Real-Time Integration

Raymond should implement live dashboard updates.

## Supported Options

Use either:

- WebSocket
- Server-Sent Events

## Required File

```text
lib/websocket-client.ts
```

or

```text
lib/sse-client.ts
```

## Real-Time Events

Frontend should listen for:

```text
new_signal
new_correlated_event
new_risk_score
new_alert
system_status_update
```

## UI Behavior

When a new event arrives:

- update relevant dashboard data
- show notification toast
- refresh affected page section
- avoid full page reload

---

# 9. State Management

Raymond should implement global state for:

- current dashboard filters
- selected entity
- selected risk level
- selected time range
- live alerts
- sidebar state
- user preferences

Suggested options:

- Zustand
- React Context

---

# 10. UI Behavior Requirements

Every page must include:

- loading state
- error state
- empty state
- refresh action
- search or filter where relevant
- responsive layout

---

# 11. Integration With Geng Xin

Raymond should use the reusable UI components and design system created by Geng Xin.

Raymond should coordinate with Geng Xin on:

- layout structure
- chart components
- table components
- cards
- alert components
- modal components
- responsive behavior

---

# 12. Integration With Jay

Raymond needs API support from Jay for:

- source status
- scrape job status
- generated intelligence signals
- agent processing status
- system health
- monitoring statistics

Frontend API expectations from Jay:

```text
GET /health
GET /monitoring/streams
GET /monitoring/workers
GET /agents/signals
GET /scrape-jobs
```

---

# 13. Integration With Kai Zhe

Raymond needs API support from Kai Zhe for:

- correlated events
- risk scores
- graph data
- RAG answers
- analytics summaries

Frontend API expectations from Kai Zhe:

```text
GET /correlation/events
GET /risk-scores
GET /graph/entities
GET /graph/timeline/{entity_id}
POST /rag/ask
GET /analytics/overview
```

---

# 14. MVP Completion Checklist

## Project Setup

- [ ] Next.js project created
- [ ] TypeScript configured
- [ ] Tailwind CSS v4 configured
- [ ] Folder structure completed
- [ ] Dockerfile created
- [ ] Environment variables documented

## Layout

- [ ] Dashboard layout created
- [ ] Sidebar created
- [ ] Topbar created
- [ ] Responsive layout completed

## Pages

- [ ] Executive Overview page completed
- [ ] Threat Feed page completed
- [ ] Competitor Intelligence page completed
- [ ] Vendor Monitoring page completed
- [ ] Alerts page completed
- [ ] Intelligence Explorer page completed

## API Integration

- [ ] API client completed
- [ ] Dashboard overview API integrated
- [ ] Signal APIs integrated
- [ ] Risk score APIs integrated
- [ ] Correlation APIs integrated
- [ ] RAG API integrated

## Real-Time

- [ ] WebSocket or SSE client completed
- [ ] New alert event handled
- [ ] New signal event handled
- [ ] New risk score event handled
- [ ] Toast notification implemented

## UX

- [ ] Loading states completed
- [ ] Error states completed
- [ ] Empty states completed
- [ ] Filters completed
- [ ] Search completed
- [ ] Pagination completed where required

---

# 15. Suggested Build Order

## Phase 1 — Frontend Foundation

- setup Next.js
- setup Tailwind CSS v4
- setup layout
- setup routing
- setup API client

## Phase 2 — Basic Dashboard

- build sidebar and topbar
- build Executive Overview page
- create metric cards
- connect overview API

## Phase 3 — Intelligence Pages

- build Threat Feed
- build Competitor Intelligence
- build Vendor Monitoring
- build tables and filters

## Phase 4 — Risk and Correlation Views

- integrate risk score API
- integrate correlated events API
- create event detail views

## Phase 5 — RAG Explorer

- build RAG question interface
- display AI answer
- display supporting evidence
- display confidence score

## Phase 6 — Real-Time Updates

- implement WebSocket or SSE client
- implement live updates
- implement toast notifications

## Phase 7 — Polish and Testing

- improve responsiveness
- fix UI bugs
- add loading and error states
- test all pages

---

# 16. Final Deliverable

Raymond’s final deliverable is a working SentinelX frontend dashboard that can:

1. display executive intelligence overview
2. show live threat feeds
3. show competitor intelligence
4. show vendor risk monitoring
5. show alerts and notifications
6. connect to backend FastAPI services
7. render correlated events and risk scores
8. support RAG-based intelligence exploration
9. update in real time using WebSocket or SSE
10. provide a clean and responsive user experience

This is the main user-facing interface of SentinelX.
